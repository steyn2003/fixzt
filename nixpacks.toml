[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x", "php83", "php83Packages.composer", "php83Extensions.pdo_pgsql", "nginx"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader", "npm ci", "npm run build", "php artisan config:cache", "php artisan route:cache", "php artisan view:cache"]

[start]
cmd = "php artisan migrate --force && (php artisan queue:work &) && nginx -g 'daemon off;'"
